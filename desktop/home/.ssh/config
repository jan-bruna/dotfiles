#systemctl --user enable --now ssh-tpm-agent.socket
#usermod -aG tss $USER
#chmod 600 ~/.ssh/id_ecdsa.tpm
#chmod 600 ~/.ssh/id_ecdsa.pub

Host github.com
    User git
    IdentityAgent /run/user/1000/ssh-tpm-agent.sock
    IdentityFile ~/.ssh/github.pub
    IdentitiesOnly yes

Host codeberg.org
    User git
    IdentityAgent /run/user/1000/ssh-tpm-agent.sock
    IdentityFile ~/.ssh/codeberg.pub
    IdentitiesOnly yes
